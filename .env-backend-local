# Puertos/hosts usados por el compose
FRONTEND_HOST=72.60.169.104
FRONTEND_PORT=3000
BACKEND_PATH=/backend

# URLs (si tu app las usa para CORS, etc.)
BACKEND_URL=http://${FRONTEND_HOST}:${FRONTEND_PORT}${BACKEND_PATH}
FRONTEND_URL=http://${FRONTEND_HOST}:${FRONTEND_PORT}

# DB
DB_DIALECT=postgres
DB_HOST=postgres
DB_PORT=5432
DB_USER=ticketz
DB_PASS=supersecret         # <— AGREGA ESTO
DB_NAME=ticketz
DB_TIMEZONE=-03:00

# Redis
REDIS_URI=redis://redis:6379

# Otros (si aplica)
USER_LIMIT=10000
CONNECTIONS_LIMIT=100000
CLOSED_SEND_BY_ME=true
SOCKET_ADMIN=true
TZ=America/Sao_Paulo
